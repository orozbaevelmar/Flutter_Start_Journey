import 'package:flutter/material.dart';

class Jalal_Abad extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Jalal-Abad'),
    );
  }
}
